#!/bin/sh

all_devs=$(lsblk -A -l -o name,path,rm,mountpoint,type,fstype,size,vendor,model)

echo "$all_devs"
echo

awk_script='$1 == NAME { print $0 }
            $3 == 1 && $4 == "" { print $0 }'

awk_script='{print $4}'

echo "$all_devs" | awk -F ' ' "$awk_script" | fzfmenu

