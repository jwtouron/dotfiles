#!/usr/bin/env bash

if ! command -v rclone >/dev/null; then
    echo ERROR: rclone not found
    exit 1
fi

if ! rclone config show | grep -q '\[dropbox\]'; then
    rclone config create dropbox dropbox
fi

rclone mount dropbox: "$HOME/Dropbox" >/dev/null
