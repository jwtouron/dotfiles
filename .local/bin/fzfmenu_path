#!/bin/sh

cachedir="${XDG_CACHE_HOME:-"$HOME/.cache"}"
cache="$cachedir/fzfmenu_run"

[ ! -e "$cachedir" ] && mkdir -p "$cachedir"

IFS=:
if stest.c -dqr -n "$cache" $PATH; then
	stest.c -flx $PATH | sort -u | tee "$cache"
else
	cat "$cache"
fi
