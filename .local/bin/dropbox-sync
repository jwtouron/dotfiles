#!/usr/bin/env bash

set -euxo pipefail

if ! command -v rclone >/dev/null; then
    echo ERROR: rclone not found
    exit 1
fi

rclone bisync ~/Documents/Notes mydropbox:Notes --verbose "$@"

# vim: set ft=bash
