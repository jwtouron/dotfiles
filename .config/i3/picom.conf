backend = "glx";
vsync = true;

blur:
{
    method = "dual_kawase";
    strength = 7;
};

# blur-background = true;
# blur-background-fixed = true;

# opacity-rule = [
#     "50:class_g = 'Polybar'"
# ]

rules = (
    { match = "class_g = 'Polybar'"; blur-background = true; blur-background-fixed = true;}
);
